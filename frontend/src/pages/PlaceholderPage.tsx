export function PlaceholderPage({ title }: { title: string }) {
  return (
    <section className="placeholder liquid-panel">
      <h1>{title}</h1>
      <p>Pagina placeholder pronta per l'implementazione successiva.</p>
    </section>
  );
}
